import ExecutionRoom from "../../components/ExecutionRoom.jsx";

export const metadata = {
  title: "Execution room · Referi",
  description:
    "Run a bounded treasury-rebalance scenario against a mock fixture and watch incident memory turn into an enforced Execution Passport.",
};

export default function ExecutionPage() {
  return <ExecutionRoom />;
}
